@{
}

<div class="form-horizontal" id="bindVue">
    <div class="form-group" v-bind:class="{'has-error': (showError && !storeId)}">
        <label class="control-label col-xs-3 require">商户编码：</label>
        <div class="col-xs-8">
            <input class="form-control" v-model.trim="storeId" />
        </div>
    </div>
    <div class="form-group" v-bind:class="{'has-error': (showError && !code)}">
        <label class="control-label col-xs-3">登录帐号：</label>
        <div class="col-xs-8">
            <input class="form-control" v-model.trim="code" />
        </div>
    </div>
    <div class="form-group" v-bind:class="{'has-error': (showError && !password)}">
        <label class="control-label col-xs-3">登录密码：</label>
        <div class="col-xs-8">
            <input class="form-control" type="password" v-model.trim="password" />
        </div>
    </div>
</div>
